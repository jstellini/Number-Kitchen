// The recipe book. A recipe is data: the steps in order, by name from Steps. A new recipe that
// reuses existing steps needs no new code.
const RECIPES = [
  {
    id: 'pizza',
    name: 'Pizza',
    steps: ['mix', 'stir', 'roll', 'sauce', 'toppings', 'bake', 'cut', 'serve'],
  },
];

// The customers. Each is drawn in art.js with an idle and a happy face.
const CUSTOMERS = ['bear', 'bunny', 'cat', 'panda'];
